{
  "name": "expenseflow",
  "version": "1.0.0",
  "description": "ExpenseFlow — Personal Finance Tracker",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp -r css www/ && cp -r js www/ && cp -r icons www/ && cp manifest.json www/ && cp sw.js www/ && echo 'Build done'",
    "sync": "npm run build && cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
